const PageDescription = ({ children }) => {
  return (
    <p className="max-w-[50rem] w-full text-white text-center text-pretty text-base">
      {children}
    </p>
  );
};

export default PageDescription;
